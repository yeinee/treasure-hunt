<%= form_for(@engage_state) do |f| %>
  <% if @engage_state.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@engage_state.errors.count, "error") %> prohibited this engage_state from being saved:</h2>

      <ul>
      <% @engage_state.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <input type="hidden" value=<%= @post.id %> name="engage_state[post_id]">
  <input type="hidden" value=<%= current_user.id %> name="engage_state[user_id]">
  <input type="hidden" value=<%= @q_num %> name="engage_state[question_number]">

  <div class="input-group text-center">
    <input type="text" id="engage_state_answer" name="engage_state[answer]" class="form-control input-md">
    <span class="input-group-btn">
      <button class="btn btn-default"><i class="glyphicon glyphicon-heart"></i></button>
    </span>
  </div>
<% end %>
